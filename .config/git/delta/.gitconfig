[interactive]
  diffFilter = delta --color-only

[include]
  path = ./themes.gitconfig
  path = ./active-theme.gitconfig

[delta]
  navigate = true    # use n and N to move between diff sections
  line-numbers = true
  side-by-side = true
